app_bo_files_routes:
    resource: './back/'
    type:     directory

back.index:
  path: /index
  controller: App\Controller\BackController::index

back.download:
  path: /download/{fileName}
  controller: App\Controller\BackController::download