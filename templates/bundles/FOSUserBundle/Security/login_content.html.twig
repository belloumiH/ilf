{% trans_default_domain 'FOSUserBundle' %}

{#{% if error %}#}
    {#<div>{{ error.messageKey|trans(error.messageData, 'security') }}</div>#}
{#{% endif %}#}

{#<form action="{{ path("fos_user_security_check") }}" method="post">#}
    {#{% if csrf_token %}#}
        {#<input type="hidden" name="_csrf_token" value="{{ csrf_token }}" />#}
    {#{% endif %}#}

    {#<label for="username">{{ 'security.login.username'|trans }}</label>#}
    {#<input type="text" id="username" name="_username" value="{{ last_username }}" required="required" autocomplete="username" />#}

    {#<label for="password">{{ 'security.login.password'|trans }}</label>#}
    {#<input type="password" id="password" name="_password" required="required" autocomplete="current-password" />#}

    {#<input type="checkbox" id="remember_me" name="_remember_me" value="on" />#}
    {#<label for="remember_me">{{ 'security.login.remember_me'|trans }}</label>#}

    {#<input type="submit" id="_submit" name="_submit" value="{{ 'security.login.submit'|trans }}" />#}
{#</form>#}

<div class="container-fluid">
    <div class="row">
        <div class="col">
            <h1 class="h2 mb-3 red text-center mt-5">Welcome to the Kia management interface</h1>
            <p class="h5 text-center">Welcome</p>
        </div>
    </div>
    <div class="row">
        <div class="col">
            <form method="post" action="{{ path("fos_user_security_check") }}" class="mt-4 form-login needs-validation" novalidate>
                {% if csrf_token %}
                    <input type="hidden" name="_csrf_token" value="{{ csrf_token }}" />
                {% endif %}
                <div class="form-group">
                    <label for="login">User Name</label>
                    <input type="text" class="form-control" name="_username" id="username" required="required" autocomplete="username">
                    <div class="invalid-feedback">
                        {% if error %}
                            put User Name
                        {% endif %}
                    </div>
                </div>
                <div class="form-group">
                    <label for="password">Your Passe Word</label>
                    <input  class="form-control" type="password" id="password" name="_password" required="required" autocomplete="current-password">
                    <div class="invalid-feedback">
                        {% if error %}
                            put Passe Word
                        {% endif %}
                    </div>
                </div>
                {#<div class="form-group has-feedback">#}
                    {#<select name="locale" class="form-control">#}
                        {#<option value="">Language</option>#}
                        {#<option value="en">English</option>#}
                        {#<option value="fr">Français</option>#}
                    {#</select>#}
                {#</div>#}
                <div class="text-center">
                    <button class="btn btn-primary px-5" type="submit" id="_submit" name="_submit" >Log in</button>
                </div>
            </form>
        </div>
    </div>
    {#<div class="row">#}
    {#<div class="col text-center">#}
    {#<img src="{{ asset('manager/img/kia-logo-reprise.png') }}" alt="" class="background-image">#}
    {#</div>#}
    {#</div>#}
</div>
